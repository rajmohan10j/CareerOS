function render() {
  return `
    <div class="page jobs-page">
      <h1 class="page-title">Job Tracker</h1>
      <p class="page-subtitle">Track job opportunities and evaluate fit with your profile.</p>
      <div class="page-placeholder">
        <div class="placeholder-icon">💼</div>
        <p>Job tracking interface will be available here.</p>
        <p class="placeholder-hint">Supports job evaluation, skill matching, and fit scoring via backend AI.</p>
      </div>
    </div>
  `;
}

export default { render };
