function render() {
  return `
    <div class="page extension-page">
      <h1 class="page-title">Browser Extension</h1>
      <p class="page-subtitle">Manage the CareerOS browser extension for job form autofill.</p>
      <div class="page-placeholder">
        <div class="placeholder-icon">🧩</div>
        <p>Browser extension management interface will be available here.</p>
        <p class="placeholder-hint">The extension runs independently in your browser. This page will show status and configuration.</p>
      </div>
    </div>
  `;
}

export default { render };
